describe('My First Test', () => { // Permite darle un nombre a un grupo de Test que estan dentro de un bloque
  it('Does not do much!', () => { // It nos da la posibilidad de darle un nombre al test
    expect(true).to.equal(true) // Nos permite comparar con algo y determinar si cumple o no cumple
  })
})

